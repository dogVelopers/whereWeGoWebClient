import { Theme } from '@emotion/react';

const theme: Theme = {
  color: {
    bgColor: 'var(--bg-color)',
    textColor: 'var(--text-color)',
    textWhiteColor: 'var(--text-white-color)',
    brandColor: 'var(--brand-color)',
    footerBgColor: 'var(--footer-bg-color)',
    footerTextColor: 'var(--footer-text-color)',
  },
  padding: {
    layoutPadding: 'var(--layout-padding)',
  },
  shadow: {
    default: 'var(--default-shadow)',
  },
};

export default theme;
